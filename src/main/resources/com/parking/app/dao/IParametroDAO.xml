<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parking.app.dao.IParametroDAO">

    <select id="find" resultType="TParametro">
        SELECT * FROM _parametros WHERE codigo = #{codigo}
    </select>

</mapper>